<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="92dp"
                android:background="@drawable/ripple_round_white"
                tools:background="#8ecccb">

    <ImageView
        android:id="@+id/iv_cook"
        android:layout_width="90dp"
        android:layout_height="60dp"
        android:layout_margin="@dimen/activity_horizontal_margin"
        android:scaleType="centerCrop"
        tools:src="@mipmap/ic_launcher"/>

    <TextView
        android:id="@+id/tv_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/iv_cook"
        android:layout_marginTop="4dp"
        android:layout_toRightOf="@+id/iv_cook"
        android:textColor="@color/colorPrimaryText"
        android:textSize="16sp"
        tools:text="红烧排骨"/>

    <TextView
        android:id="@+id/tv_info"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/iv_cook"
        android:layout_marginBottom="4dp"
        android:layout_marginRight="16dp"
        android:layout_toRightOf="@+id/iv_cook"
        android:ellipsize="end"
        android:maxLines="1"
        tools:text="面粉，橄榄油，排骨，老抽上的说法是否的奋斗奋斗"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="0.6dp"
        android:layout_alignParentBottom="true"
        android:background="@color/colorDivider"/>

</RelativeLayout>